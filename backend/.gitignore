venv/
__pycache__/
*.pyc
*.pyo
.env
app.db
*.db

# Keep .pkl files in git so Render doesn't retrain on every deploy
# Comment out below if you want to retrain on each deploy instead
# app/ml/models/*.pkl
